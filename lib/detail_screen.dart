import 'package:flutter/material.dart';
import 'bag.dart';
import 'home_screen.dart';

class DetailScreen extends StatelessWidget {
  const DetailScreen({super.key, required bag});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}